Swift:

func squares(_ num1: Int, _ num2: Int) -> Int {
    let root1 = sqrt(Double(num1))
    let root2 = sqrt(Double(num2))
    return Int(floor(root2)) - Int(ceil(root1)) + 1
}

Javascript:

function squares(num1, num2) {
    let root1 = Math.sqrt(num1);
    let root2 = Math.sqrt(num2);
    return Math.floor(root2) - Math.ceil(root1) + 1;
}
